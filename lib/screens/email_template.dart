import 'package:flutter/material.dart';

class EmailTemplateScreen extends StatelessWidget {
  const EmailTemplateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Email Template Screen'),
      ),
    );
  }
}
