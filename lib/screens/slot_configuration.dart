import 'package:flutter/material.dart';

class SlotConfiguration extends StatelessWidget {
  const SlotConfiguration({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Slot Configuration Screen'),
      ),
    );
  }
}
